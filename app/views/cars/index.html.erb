<h1>Listing cars</h1>

<table>
  <tr>
    <th>Brand Name</th>
    <th>Model</th>
    <th>Year</th>
    <th>Release Date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cars.each do |car| %>
  <tr>
    <th><%= car.brand.name %></th>
    <th><%= car.brand_model_name %></th>
    <th><%= car.year %></th>
    <th><%= car.release_date %></th>
    <td><%= link_to 'Show', car %></td>
    <td><%= link_to 'Edit', edit_car_path(car) %></td>
    <td><%= link_to 'Destroy', car, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Car', new_car_path %>
